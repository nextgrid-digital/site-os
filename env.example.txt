# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Optional legacy alias
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Required for Site-OS server-side audit writes (bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=

# Google OAuth (Search Console + GA4 readonly — operator connect)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Local:
GOOGLE_REDIRECT_URI=http://localhost:3000/api/google/oauth/callback
# Production (Vercel): https://site-os-opal.vercel.app/api/google/oauth/callback

# End-user Google sign-in is configured in the Supabase dashboard (not env):
# Authentication → Providers → Google → enable + Client ID/Secret
# Google Cloud redirect URI: https://<PROJECT_REF>.supabase.co/auth/v1/callback
# Authentication → URL configuration:
#   Site URL = production origin (NOT localhost) on the live project
#   Redirect allow list: localhost + https://site-os-opal.vercel.app/auth/callback
# Authentication → Providers → Email → disable “Confirm email” for password signup
# See docs/auth-setup.md

# App
# Local:
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Production (Vercel): https://site-os-opal.vercel.app
OPERATOR_EMAIL=hello@nextgrid.digital

# AI / AEO layer (server-only)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite
# Optional: request timeout in ms (default 120000)
# GEMINI_TIMEOUT_MS=120000
# Optional override
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
# Heavier model (slower; may timeout on large sites):
# GEMINI_MODEL=gemini-3.5-flash
