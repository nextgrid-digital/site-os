/** @deprecated Prefer the operator layout shell. Kept as a no-op wrapper for gradual migration. */
export function OperatorShell({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
